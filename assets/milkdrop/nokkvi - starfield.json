{
 "version": 2,
 "baseVals": {
  "gammaadj": 1.0,
  "decay": 1.0,
  "echo_zoom": 1.0,
  "echo_alpha": 0.0,
  "wave_mode": 0,
  "additivewave": 1,
  "wave_a": 0.0,
  "wave_scale": 0.8,
  "wave_smoothing": 0.7,
  "zoom": 1.0,
  "rot": 0.0,
  "warp": 0.0,
  "wave_r": 1.0,
  "wave_g": 1.0,
  "wave_b": 1.0,
  "ob_size": 0.0,
  "ob_a": 0.0,
  "ib_size": 0.0,
  "ib_a": 0.0,
  "mv_a": 0.0,
  "darken_center": 0,
  "brighten": 0,
  "darken": 0,
  "solarize": 0,
  "invert": 0
 },
 "shapes": [
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": ""
  },
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": ""
  },
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": ""
  },
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": ""
  }
 ],
 "waves": [
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": "",
   "point_eqs_eel": ""
  },
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": "",
   "point_eqs_eel": ""
  },
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": "",
   "point_eqs_eel": ""
  },
  {
   "baseVals": {
    "enabled": 0
   },
   "init_eqs_eel": "",
   "frame_eqs_eel": "",
   "point_eqs_eel": ""
  }
 ],
 "init_eqs_eel": "pulse = 0; pop = 0; travel = 0; roll = 0; speed = 0; twist = 0.015; spiral = 0; px = 0.35; py = 0.08; prad = 0.16; pring = 1; ptilt = 0.3; pcol = 0.35; pspin = 0;",
 "frame_eqs_eel": "kick = max(bass - bass_att, 0);\npulse = max(pulse * 0.86, min(kick * 0.9, 1));\nq3 = pulse;\npop = max(pop * 0.78, min(max(bass - 1.2, 0) * 0.55, 1.4));\nq5 = pop;\nspeed = speed * 0.9 + 0.1 * (0.012 + 0.03 * min(bass_att, 2) + 0.12 * q3 + 0.1 * pop);\ntravel = travel + speed * 0.25;\nroll = roll + 0.0012 * (mid_att - 0.8) + 0.004 * q3 * sign(sin(time * 0.05));\nq1 = travel;\nq2 = speed * 6;\nq4 = roll;\ntwist = twist * 0.97 + 0.03 * (0.02 * sin(time * 0.11) + 0.012 * (mid_att - 1) + 0.03 * pop * sign(sin(time * 0.11)));\nspiral = spiral + twist * 60 * speed;\nq10 = twist;\nq11 = spiral;\ndt = 1 / max(fps, 1);\npx = px - (0.03 + 0.25 * speed) * dt;\nlim = 0.5 / aspectx + prad + 0.1;\nwrap = below(px, -lim);\nprad = if(wrap, 0.1 + 0.12 * rand(100) / 100, prad);\npx = if(wrap, 0.5 / aspectx + prad + 0.1, px);\npy = if(wrap, -0.28 + 0.56 * rand(100) / 100, py);\npring = if(wrap, above(rand(100), 35), pring);\nptilt = if(wrap, 0.18 + 0.3 * rand(100) / 100, ptilt);\npcol = if(wrap, rand(100) / 100, pcol);\npspin = pspin + dt * 0.05;\nq12 = px;\nq13 = py;\nq14 = prad;\nq15 = pring;\nq16 = ptilt;\nq17 = pcol;\nq18 = pspin;",
 "pixel_eqs_eel": "",
 "warp": " shader_body {\n  vec2 s = texsize.xy / min(texsize.x, texsize.y);\n  vec2 p = (uv_orig - 0.5) * s;\n  float cr = cos(q4); float sr = sin(q4);\n  vec2 pr = vec2(p.x * cr - p.y * sr, p.x * sr + p.y * cr);\n  vec3 stars = vec3(0.0);\n  {\n    float z = fract(0.0 / 6.0 + q1);\n    float scale = mix(26.0, 0.6, z);\n    float fade = smoothstep(0.0, 0.25, z) * smoothstep(1.0, 0.85, z);\n    float tw = q11 * (1.0 - z) * 2.2;\n    vec2 prt = vec2(pr.x * cos(tw) - pr.y * sin(tw), pr.x * sin(tw) + pr.y * cos(tw));\n    vec2 g = prt * scale + vec2(0.0, 0.0);\n    vec2 id = floor(g);\n    vec2 f = fract(g) - 0.5;\n    float h = fract(sin(dot(id, vec2(127.1, 311.7))) * 43758.5453);\n    float h2 = fract(h * 91.3);\n    vec2 d = f - (vec2(h, h2) - 0.5) * 0.5;\n    float size = mix(0.012, 0.04, h2) * (0.6 + z);\n    float dist = length(d);\n    float core = smoothstep(size, 0.0, dist);\n    float glow = size * 0.5 / (dist + 0.02) * smoothstep(0.2, 0.0, dist);\n    float twinkle = 0.75 + 0.25 * sin(time * (3.0 + h * 5.0) + h * 40.0);\n    float spec = get_fft(0.02 + h2 * 0.6);\n    float b = (core * 1.6 + glow * 0.5) * fade * twinkle * step(0.55, h) * (0.45 + 2.4 * spec);\n  float sc0_x = clamp(h2, 0.0, 1.0) * 5.0;\n  vec3 sc0 = mix(NOKKVI_RAMP0, NOKKVI_RAMP1, clamp(sc0_x, 0.0, 1.0));\n  sc0 = mix(sc0, NOKKVI_RAMP2, clamp(sc0_x - 1.0, 0.0, 1.0));\n  sc0 = mix(sc0, NOKKVI_RAMP3, clamp(sc0_x - 2.0, 0.0, 1.0));\n  sc0 = mix(sc0, NOKKVI_RAMP4, clamp(sc0_x - 3.0, 0.0, 1.0));\n  sc0 = mix(sc0, NOKKVI_RAMP5, clamp(sc0_x - 4.0, 0.0, 1.0));\n    stars += mix(sc0, NOKKVI_TEXT, core * 0.7) * b;\n  }\n  {\n    float z = fract(1.0 / 6.0 + q1);\n    float scale = mix(26.0, 0.6, z);\n    float fade = smoothstep(0.0, 0.25, z) * smoothstep(1.0, 0.85, z);\n    float tw = q11 * (1.0 - z) * 2.2;\n    vec2 prt = vec2(pr.x * cos(tw) - pr.y * sin(tw), pr.x * sin(tw) + pr.y * cos(tw));\n    vec2 g = prt * scale + vec2(37.1, 91.7);\n    vec2 id = floor(g);\n    vec2 f = fract(g) - 0.5;\n    float h = fract(sin(dot(id, vec2(127.1, 311.7))) * 43758.5453);\n    float h2 = fract(h * 91.3);\n    vec2 d = f - (vec2(h, h2) - 0.5) * 0.5;\n    float size = mix(0.012, 0.04, h2) * (0.6 + z);\n    float dist = length(d);\n    float core = smoothstep(size, 0.0, dist);\n    float glow = size * 0.5 / (dist + 0.02) * smoothstep(0.2, 0.0, dist);\n    float twinkle = 0.75 + 0.25 * sin(time * (3.0 + h * 5.0) + h * 40.0);\n    float spec = get_fft(0.02 + h2 * 0.6);\n    float b = (core * 1.6 + glow * 0.5) * fade * twinkle * step(0.55, h) * (0.45 + 2.4 * spec);\n  float sc1_x = clamp(h2, 0.0, 1.0) * 5.0;\n  vec3 sc1 = mix(NOKKVI_RAMP0, NOKKVI_RAMP1, clamp(sc1_x, 0.0, 1.0));\n  sc1 = mix(sc1, NOKKVI_RAMP2, clamp(sc1_x - 1.0, 0.0, 1.0));\n  sc1 = mix(sc1, NOKKVI_RAMP3, clamp(sc1_x - 2.0, 0.0, 1.0));\n  sc1 = mix(sc1, NOKKVI_RAMP4, clamp(sc1_x - 3.0, 0.0, 1.0));\n  sc1 = mix(sc1, NOKKVI_RAMP5, clamp(sc1_x - 4.0, 0.0, 1.0));\n    stars += mix(sc1, NOKKVI_TEXT, core * 0.7) * b;\n  }\n  {\n    float z = fract(2.0 / 6.0 + q1);\n    float scale = mix(26.0, 0.6, z);\n    float fade = smoothstep(0.0, 0.25, z) * smoothstep(1.0, 0.85, z);\n    float tw = q11 * (1.0 - z) * 2.2;\n    vec2 prt = vec2(pr.x * cos(tw) - pr.y * sin(tw), pr.x * sin(tw) + pr.y * cos(tw));\n    vec2 g = prt * scale + vec2(74.2, 183.4);\n    vec2 id = floor(g);\n    vec2 f = fract(g) - 0.5;\n    float h = fract(sin(dot(id, vec2(127.1, 311.7))) * 43758.5453);\n    float h2 = fract(h * 91.3);\n    vec2 d = f - (vec2(h, h2) - 0.5) * 0.5;\n    float size = mix(0.012, 0.04, h2) * (0.6 + z);\n    float dist = length(d);\n    float core = smoothstep(size, 0.0, dist);\n    float glow = size * 0.5 / (dist + 0.02) * smoothstep(0.2, 0.0, dist);\n    float twinkle = 0.75 + 0.25 * sin(time * (3.0 + h * 5.0) + h * 40.0);\n    float spec = get_fft(0.02 + h2 * 0.6);\n    float b = (core * 1.6 + glow * 0.5) * fade * twinkle * step(0.55, h) * (0.45 + 2.4 * spec);\n  float sc2_x = clamp(h2, 0.0, 1.0) * 5.0;\n  vec3 sc2 = mix(NOKKVI_RAMP0, NOKKVI_RAMP1, clamp(sc2_x, 0.0, 1.0));\n  sc2 = mix(sc2, NOKKVI_RAMP2, clamp(sc2_x - 1.0, 0.0, 1.0));\n  sc2 = mix(sc2, NOKKVI_RAMP3, clamp(sc2_x - 2.0, 0.0, 1.0));\n  sc2 = mix(sc2, NOKKVI_RAMP4, clamp(sc2_x - 3.0, 0.0, 1.0));\n  sc2 = mix(sc2, NOKKVI_RAMP5, clamp(sc2_x - 4.0, 0.0, 1.0));\n    stars += mix(sc2, NOKKVI_TEXT, core * 0.7) * b;\n  }\n  {\n    float z = fract(3.0 / 6.0 + q1);\n    float scale = mix(26.0, 0.6, z);\n    float fade = smoothstep(0.0, 0.25, z) * smoothstep(1.0, 0.85, z);\n    float tw = q11 * (1.0 - z) * 2.2;\n    vec2 prt = vec2(pr.x * cos(tw) - pr.y * sin(tw), pr.x * sin(tw) + pr.y * cos(tw));\n    vec2 g = prt * scale + vec2(111.3, 275.1);\n    vec2 id = floor(g);\n    vec2 f = fract(g) - 0.5;\n    float h = fract(sin(dot(id, vec2(127.1, 311.7))) * 43758.5453);\n    float h2 = fract(h * 91.3);\n    vec2 d = f - (vec2(h, h2) - 0.5) * 0.5;\n    float size = mix(0.012, 0.04, h2) * (0.6 + z);\n    float dist = length(d);\n    float core = smoothstep(size, 0.0, dist);\n    float glow = size * 0.5 / (dist + 0.02) * smoothstep(0.2, 0.0, dist);\n    float twinkle = 0.75 + 0.25 * sin(time * (3.0 + h * 5.0) + h * 40.0);\n    float spec = get_fft(0.02 + h2 * 0.6);\n    float b = (core * 1.6 + glow * 0.5) * fade * twinkle * step(0.55, h) * (0.45 + 2.4 * spec);\n  float sc3_x = clamp(h2, 0.0, 1.0) * 5.0;\n  vec3 sc3 = mix(NOKKVI_RAMP0, NOKKVI_RAMP1, clamp(sc3_x, 0.0, 1.0));\n  sc3 = mix(sc3, NOKKVI_RAMP2, clamp(sc3_x - 1.0, 0.0, 1.0));\n  sc3 = mix(sc3, NOKKVI_RAMP3, clamp(sc3_x - 2.0, 0.0, 1.0));\n  sc3 = mix(sc3, NOKKVI_RAMP4, clamp(sc3_x - 3.0, 0.0, 1.0));\n  sc3 = mix(sc3, NOKKVI_RAMP5, clamp(sc3_x - 4.0, 0.0, 1.0));\n    stars += mix(sc3, NOKKVI_TEXT, core * 0.7) * b;\n  }\n  {\n    float z = fract(4.0 / 6.0 + q1);\n    float scale = mix(26.0, 0.6, z);\n    float fade = smoothstep(0.0, 0.25, z) * smoothstep(1.0, 0.85, z);\n    float tw = q11 * (1.0 - z) * 2.2;\n    vec2 prt = vec2(pr.x * cos(tw) - pr.y * sin(tw), pr.x * sin(tw) + pr.y * cos(tw));\n    vec2 g = prt * scale + vec2(148.4, 366.8);\n    vec2 id = floor(g);\n    vec2 f = fract(g) - 0.5;\n    float h = fract(sin(dot(id, vec2(127.1, 311.7))) * 43758.5453);\n    float h2 = fract(h * 91.3);\n    vec2 d = f - (vec2(h, h2) - 0.5) * 0.5;\n    float size = mix(0.012, 0.04, h2) * (0.6 + z);\n    float dist = length(d);\n    float core = smoothstep(size, 0.0, dist);\n    float glow = size * 0.5 / (dist + 0.02) * smoothstep(0.2, 0.0, dist);\n    float twinkle = 0.75 + 0.25 * sin(time * (3.0 + h * 5.0) + h * 40.0);\n    float spec = get_fft(0.02 + h2 * 0.6);\n    float b = (core * 1.6 + glow * 0.5) * fade * twinkle * step(0.55, h) * (0.45 + 2.4 * spec);\n  float sc4_x = clamp(h2, 0.0, 1.0) * 5.0;\n  vec3 sc4 = mix(NOKKVI_RAMP0, NOKKVI_RAMP1, clamp(sc4_x, 0.0, 1.0));\n  sc4 = mix(sc4, NOKKVI_RAMP2, clamp(sc4_x - 1.0, 0.0, 1.0));\n  sc4 = mix(sc4, NOKKVI_RAMP3, clamp(sc4_x - 2.0, 0.0, 1.0));\n  sc4 = mix(sc4, NOKKVI_RAMP4, clamp(sc4_x - 3.0, 0.0, 1.0));\n  sc4 = mix(sc4, NOKKVI_RAMP5, clamp(sc4_x - 4.0, 0.0, 1.0));\n    stars += mix(sc4, NOKKVI_TEXT, core * 0.7) * b;\n  }\n  {\n    float z = fract(5.0 / 6.0 + q1);\n    float scale = mix(26.0, 0.6, z);\n    float fade = smoothstep(0.0, 0.25, z) * smoothstep(1.0, 0.85, z);\n    float tw = q11 * (1.0 - z) * 2.2;\n    vec2 prt = vec2(pr.x * cos(tw) - pr.y * sin(tw), pr.x * sin(tw) + pr.y * cos(tw));\n    vec2 g = prt * scale + vec2(185.5, 458.5);\n    vec2 id = floor(g);\n    vec2 f = fract(g) - 0.5;\n    float h = fract(sin(dot(id, vec2(127.1, 311.7))) * 43758.5453);\n    float h2 = fract(h * 91.3);\n    vec2 d = f - (vec2(h, h2) - 0.5) * 0.5;\n    float size = mix(0.012, 0.04, h2) * (0.6 + z);\n    float dist = length(d);\n    float core = smoothstep(size, 0.0, dist);\n    float glow = size * 0.5 / (dist + 0.02) * smoothstep(0.2, 0.0, dist);\n    float twinkle = 0.75 + 0.25 * sin(time * (3.0 + h * 5.0) + h * 40.0);\n    float spec = get_fft(0.02 + h2 * 0.6);\n    float b = (core * 1.6 + glow * 0.5) * fade * twinkle * step(0.55, h) * (0.45 + 2.4 * spec);\n  float sc5_x = clamp(h2, 0.0, 1.0) * 5.0;\n  vec3 sc5 = mix(NOKKVI_RAMP0, NOKKVI_RAMP1, clamp(sc5_x, 0.0, 1.0));\n  sc5 = mix(sc5, NOKKVI_RAMP2, clamp(sc5_x - 1.0, 0.0, 1.0));\n  sc5 = mix(sc5, NOKKVI_RAMP3, clamp(sc5_x - 2.0, 0.0, 1.0));\n  sc5 = mix(sc5, NOKKVI_RAMP4, clamp(sc5_x - 3.0, 0.0, 1.0));\n  sc5 = mix(sc5, NOKKVI_RAMP5, clamp(sc5_x - 4.0, 0.0, 1.0));\n    stars += mix(sc5, NOKKVI_TEXT, core * 0.7) * b;\n  }\n  vec2 cs = (uv - 0.5) * s;\n  float sw = clamp(q10 / 0.02, -1.0, 1.0) * (0.004 + 0.02 * q2) * (0.4 + 1.1 * length(cs));\n  float zm = 1.0 + 0.012 + q2 * 0.05;\n  vec2 c1 = vec2(cs.x * cos(sw) - cs.y * sin(sw), cs.x * sin(sw) + cs.y * cos(sw));\n  vec2 back = c1 / s / zm + 0.5;\n  float sh = sw * 0.5;\n  vec2 c2 = vec2(cs.x * cos(sh) - cs.y * sin(sh), cs.x * sin(sh) + cs.y * cos(sh));\n  vec2 half_back = c2 / s / sqrt(zm) + 0.5;\n  float keep = clamp(0.8 + q2 * 0.6, 0.8, 0.95);\n  vec3 fb = max(texture(sampler_main, back).xyz, texture(sampler_main, half_back).xyz * 0.92) * keep;\n  ret = max(fb, stars);\n }",
 "comp": " shader_body {\n  vec2 s = texsize.xy / min(texsize.x, texsize.y);\n  vec2 p = (uv - 0.5) * s;\n  float grain = texture(sampler_noise_lq, uv * texsize.xy / 256.0 + rand_frame.xy).x;\n  vec3 col = NOKKVI_BG;\n  col += texture(sampler_main, uv).xyz + GetBlur1(uv) * 0.2;\n  col += NOKKVI_ACCENT * exp(-length(p) * 6.0) * (0.08 + 0.45 * q3);\n  col += NOKKVI_WARM * exp(-length(p) * 14.0) * q3 * 0.35;\n  vec2 pd = p - vec2(q12, q13);\n  float pr = q14;\n  float pl = length(pd);\n  vec3 pn = vec3(pd.x, pd.y, sqrt(max(pr * pr - pl * pl, 0.0))) / pr;\n  vec3 sun = normalize(vec3(0.6, 0.45, 0.55));\n  float lam = max(dot(pn, sun), 0.0);\n  float lat = asin(clamp(pn.y, -1.0, 1.0));\n  float lon = atan(pn.x, pn.z) + q18;\n  float pb1 = texture(sampler_noise_hq, vec2(lon * 0.08 + q17, lat * 0.9 + q17 * 0.3)).x;\n  float pb2 = texture(sampler_noise_hq, vec2(lon * 0.25 + q18 * 0.3, lat * 2.5)).x;\n  float bandv = clamp(pb1 * 0.7 + pb2 * 0.3, 0.0, 1.0);\n  float pcol_x = clamp(bandv, 0.0, 1.0) * 5.0;\n  vec3 pcol = mix(NOKKVI_RAMP0, NOKKVI_RAMP1, clamp(pcol_x, 0.0, 1.0));\n  pcol = mix(pcol, NOKKVI_RAMP2, clamp(pcol_x - 1.0, 0.0, 1.0));\n  pcol = mix(pcol, NOKKVI_RAMP3, clamp(pcol_x - 2.0, 0.0, 1.0));\n  pcol = mix(pcol, NOKKVI_RAMP4, clamp(pcol_x - 3.0, 0.0, 1.0));\n  pcol = mix(pcol, NOKKVI_RAMP5, clamp(pcol_x - 4.0, 0.0, 1.0));\n  vec3 planet = pcol * (0.08 + 0.95 * lam) + NOKKVI_TEXT * pow(lam, 8.0) * 0.15;\n  float atmo = pow(1.0 - pn.z, 4.0);\n  planet += NOKKVI_HIGHLIGHT * atmo * (0.3 + 0.3 * q3) * (0.3 + 0.7 * lam);\n  float pmask = smoothstep(pr, pr - 0.003, pl);\n  col = mix(col, planet, pmask);\n  col += NOKKVI_HIGHLIGHT * smoothstep(pr * 1.25, pr, pl) * step(pr, pl) * (0.08 + 0.08 * q3);\n  vec2 rc = vec2(pd.x, pd.y / q16);\n  float rr = length(rc) / pr;\n  float ringband = texture(sampler_noise_hq, vec2(rr * 0.35 + q17, 0.5)).x;\n  float ringm = smoothstep(1.35, 1.45, rr) * smoothstep(2.3, 2.1, rr) * q15;\n  ringm *= max(step(0.0, -pd.y), step(pr, pl));\n  float sunside = clamp(dot(normalize(pd), sun.xy) * 0.5 + 0.5, 0.0, 1.0);\n  vec3 ringcol = mix(NOKKVI_SURFACE, pcol, 0.5) * ringband * (0.35 + 0.5 * sunside) + NOKKVI_TEXT * ringband * 0.1;\n  col = mix(col, ringcol, ringm * (0.55 + 0.35 * ringband));\n  col *= 0.9 + 0.1 * smoothstep(1.1, 0.2, length(p));\n  col += (grain - 0.5) * 0.012;\n  ret = col;\n }"
}